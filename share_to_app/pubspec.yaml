name: share_to_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0-271.7.beta <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Receive content via share dialog
  receive_sharing_intent: ^1.4.5

  # Logging
  l: ^3.2.0

  # Code generation
  freezed_annotation: ^2.1.0
  json_annotation: ^4.6.0

  # BLoC
  flutter_bloc: ^8.1.1

  # Navigation
  auto_route: ^5.0.1

  # code gen
  build_runner: ^2.2.0
  freezed: ^2.1.0+1
  json_serializable: ^6.3.1
  auto_route_generator: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1


flutter:
  uses-material-design: true
